# LOOM — 90-second demo script

Record once, link at the top of README. 90 seconds, no editing.

0:00–0:10 — Hook
: "This is a free, 100% client-side log analyzer. Nothing leaves the browser.
  Open the live demo, click Try 10k sample."

0:10–0:35 — The money split
: KPI strip → "Total $0.04, blockable training-only. Search-index and
  user-fetch are never counted as blockable."
  AI Matrix → "Red training = block, cyan search = rate-limit 120/min,
  blue user = never touch."

0:35–1:00 — The fix
: Module 6 → copy a Cloudflare BLOCK rule for GPTBot, show the
  training-vs-search robots.txt, show the CFO 1-pager.

1:00–1:30 — Trust
: Sampling banner on the 1GB run, verification labeled heuristic vs VERIFIED,
  EXPECTED.md pinned counts, CI badge. "Method over magic."
